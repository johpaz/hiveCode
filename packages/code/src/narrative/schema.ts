/**
 * @deprecated Hive-Code storage is bootstrapped through HiveDB collections in
 * `@johpaz/hivecode-core/storage/bootstrap`.
 *
 * Kept only so older imports of `CODE_SCHEMA` do not fail while callers are
 * migrated. New installs start from an empty HiveDB state.
 */
export const CODE_SCHEMA = ""
